import { styled } from '../../../../global/styles/theme';

export const Container = styled('TextInput', {
  borderRadius: 8,
  borderColor: '$purple5',
  borderWidth: 1,
  height: 40,
  paddingStart: 10
});
